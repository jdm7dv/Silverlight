namespace Kit3D.Windows.Media
{
    using System;

    /// <summary>
    /// Delegate which will be called when a 3D model is hit
    /// </summary>
    /// <param name="result"></param>
    /// <returns></returns>
    public delegate HitTestResultBehavior HitTestResultCallback(HitTestResult result);
}

